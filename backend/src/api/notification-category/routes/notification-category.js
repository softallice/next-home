'use strict';

/**
 * notification-category router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::notification-category.notification-category');
