'use strict';

/**
 * press-release-category controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::press-release-category.press-release-category');
