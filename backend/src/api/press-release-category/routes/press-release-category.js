'use strict';

/**
 * press-release-category router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::press-release-category.press-release-category');
